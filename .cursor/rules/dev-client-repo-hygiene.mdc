---
description: ABSOLUTE — customer app repos never ship agent skills, skill URLs, or factory rules; stack skills owned only by the engine.
alwaysApply: true
---

# Client app repo hygiene (all factory projects)

Applies to **every** product repo delivered via Hephaestus / sibling agents — not only Intown.

## Ownership

| Asset | Where it lives | In customer git? |
|-------|----------------|------------------|
| Factory skills (`dev-*`, BA/UX/QA kicks) | Engine (`.cursor/skills/`) | **Never** |
| Stack marketplace packs | Engine `.agents/skills` via `sync_stack_skills.sh` + `verify_stack_skills.sh` | **Never** |
| Project-only / conflicting skills & rules | `projects/<slug>/.cursor/{skills,rules}/` (gitignored) | **Never** |
| Skill install / verify scripts | Engine `scripts/` only | **Never** in app |
| Links to skills.sh / skill GitHub packs / `dev-agent` skill paths | Engine docs only | **Never** in app |
| Product code, OpenSpec product specs, CI, deploy | App repo | Yes |
| Product engineering standards (SP-only, secrets, ACL) | App as `docs/CODE_STANDARDS.md` (plain product language) | Yes if needed for CI/review |

## Absolute forbidden in app repos (tracked files)

- `.cursor/skills/`, `.agents/skills/`
- `docs/SKILLS*.md`, skill install runbooks, `scripts/sync_*skills*`
- Factory seat names wired as process docs that expose the agent factory (Hermes/Athena/Hephaestus skill maps) — keep those under engine / `projects/<slug>/`
- `.cursor/rules/*` that only exist for factory gates (`factory-ba-ux`, stack install, etc.)
- References like `bash ../dev-agent/scripts/sync_stack_skills.sh` or marketplace skill URLs

## How agents attach stack skills

1. Read `projects/<slug>/project.yaml` → `stack.*`
2. Engine: `bash scripts/verify_stack_skills.sh <slug> [--install]`
3. **Read** every path in `projects/<slug>/factory/stack-skills.manifest`
4. Implement in the **app** working tree — never copy packs into the app

## What the client may receive

- Source, tests, OpenSpec **product** specs, GitHub Actions / deploy scripts
- Optional `docs/CODE_STANDARDS.md` (engineering constraints of the product — no skill marketplace links, no engine paths)
- **Not** our skill catalog, install scripts, factory loop docs, or Pantheon seat playbooks

If unsure whether a file belongs in the app: **keep it in the engine or `projects/<slug>/`**.
